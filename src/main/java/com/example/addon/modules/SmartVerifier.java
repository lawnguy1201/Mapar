package com.example.addon.modules;
// hook up with litematica to verify the schematic, put up beacon beam where they are missing carpets/blocks
// need to add setting to not to scaffolding blocks for staircasing since the main use is staircase maps
public class SmartVerifier {
}
